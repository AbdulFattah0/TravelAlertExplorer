{
    "configurations": [
        {
            "name": "Debug Server",
            "type": "node",
            "request": "launch",
            "skipFiles": ["<node_internals>/**"],
            "program": "${workspaceFolder}/tests/server.js",
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}